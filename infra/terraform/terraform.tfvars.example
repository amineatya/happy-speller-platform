# Example Terraform variables file
# 
# SECURITY NOTE: Instead of creating terraform.tfvars with hardcoded values,
# use environment variables with TF_VAR_ prefix for security:
#
# export TF_VAR_minio_access_key="your-minio-access-key"
# export TF_VAR_minio_secret_key="your-minio-secret-key"
# export TF_VAR_grafana_admin_password="your-grafana-password"
#
# Or use the secure credential management system:
# source scripts/secure-credentials.sh
# init_secure_credentials
#
# Only create terraform.tfvars if you need non-sensitive overrides:

# Infrastructure deployment flags
deploy_minio = false
deploy_grafana = false

# Non-sensitive configuration
grafana_admin_user = "admin"

# =============================================================================
# SENSITIVE VALUES - DO NOT SET HERE!
# Use environment variables instead:
# =============================================================================
# minio_access_key = "DO_NOT_SET_HERE_USE_TF_VAR_minio_access_key"
# minio_secret_key = "DO_NOT_SET_HERE_USE_TF_VAR_minio_secret_key" 
# grafana_admin_password = "DO_NOT_SET_HERE_USE_TF_VAR_grafana_admin_password"
